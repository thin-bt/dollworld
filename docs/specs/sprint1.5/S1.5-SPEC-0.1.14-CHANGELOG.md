# S1.5-SPEC-0.1.14 変更記録

## 基礎

- 基礎正本: `S1.5-SPEC-0.1.13`
- 追補: `S1.5-SPEC-0.1.14`
- UI-000 checklist: `0.2.4` -> `0.2.5`
- Sprint 1.5 production実装: 未着手

## 今回修正した既知の穴

1. DEFERRED_BINDINGとSPEC_UNDEFINEDを分離
2. post-start battle execution abortのUI mapping
3. normal serializer失敗時のminimal fallback
4. errorReference生成規則
5. refreshRequiredのwire位置
6. PersonのaffiliationLabels/overallRank廃止
7. family/lineage/rank直接mapping
8. abilities/aptitudesをsurfaceValueへ固定
9. temporaryCondition表示追加
10. singular mentor推測廃止、formalMasterPersonIds化
11. TechniqueViewの曖昧JsonValue合成廃止
12. historical instructorの推測表示禁止
13. Event personId filterをentities.personIdsへ固定
14. DetailedLogを`{turnOrderLogs, actionLogs}`として扱い、paging sourceをactionLogsへ固定
15. BattleLogでrequestedAction/resolvedAction/replacementReasonを分離
16. corrupt mock latest/replay recordの500契約
17. elapsedWeeksをWorldDate.absoluteWeekへ直接binding
18. upstream schemaVersion drift gate
19. breaking DTO変更に伴い画面`apiSchemaVersion`を`0.1.0 -> 0.2.0`へ版上げ
20. CursorPayloadを0.2.0へ同期し、署名済み旧0.1.0 cursorを409 STALE_CURSORへ固定
21. PersonTemporaryConditionはPerson rootではなく、S01-008後のcommitted canonical integration recordを正本とするよう修正
22. TrainingHistoryの週次集約規則・既存48週窓・event sourceを固定
23. MockBattleReplaySnapshot/LatestRecordへ完全性hashを追加しinternal store schemaを0.2.0へ更新

## 再監査で撤回した誤検出

- `commitRunBattlePlan`と未割当eventCandidatesは矛盾しない。
  - clone内commitと共通EventEnvelope allocationは別境界。
  - 既存契約を維持。
- BattleResult schemaVersion `0.5.0`は現時点の後段正本と整合している。
- learnedTechniqueCountはacquiredだけを数える契約が0.1.13に既存。
- TechniqueCategoryは`unarmed | sword | magic`がcurrent contractであり、`martial` aliasは不要。
- request fingerprint / journal lifetime / durationMs / operationStartReadSnapshotは既に0.1.13で一意。

## 追加範囲

- GATE: 001～015
- BRIDGE: 001～119
- TX: 001～090
- PAGE: 001～014
- DET: 001～007
- FIX: 001～104
- Sprint 1.5受入条件: 1～174

## 状態

今回までに発見した既知の意味論穴は追補へ反映済み。
ただしSprint 1.5全体の横断監査は継続し、新しいSPEC_UNDEFINEDが見つかれば次版へ修正する。

## 再監査修正

- EventEnvelope.sequenceが0始まりであるため、TrainingHistory.relatedEventSequencesをnon-negative safe integerへ修正。
- Person displayNameはcanonical Person.displayName直結と明記。

- 再監査で0.1.13既存契約を再確認し、追補側の「最新48件」を撤回。`max(0,W-47)..W`の48週窓、absoluteWeek降順、能力順・TechniqueId順へ同期。

- BattleLogItemView 0.2.0のconvenience field集合を完全固定。「最低限」「必要なら追加」を削除し、raw63 fieldはsourceLogEntryを正本とした。

- ValidationResultの存在しない共通`code/sourceProcessor/canContinue`前提を撤回。generic `ok/issues[path,message]`へ統一し、queryを`code`から`status=success|failure`へ変更。

- UI-009の決定性比較projectionを固定。CLI/UI/1週×N/replayでUI metadata混入を禁止し、mock replayはBattleResult+eventCandidatesだけをexact比較。

- WorldSummaryViewのrun固定sourceとcurrent committed sourceを分離。start/reset/step/partial failureのsummary時点を固定。
- `failedWeek.validation`を失敗週public facadeの非commit validation collectionへ固定し、CommittedValidationViewStoreからの再構成を禁止。

- UI-000 checklistの旧`mock candidateと参加不可reason`記述を修正。GET候補はeligible-only/reasonなし、POST不適格は422 `BATTLE_PRE_START_FAILURE`へ固定。

- 追補本文の受入条件102～107欠落を修復し、checklist/changelogとの内部整合を回復。
- MockBattleViewのjudge/finalRngState契約を追加。judge_decisionと判定計算使用を分離し、双方unable_to_continueを正しく表現。

- MockBattleView 0.2.0全field sourceを固定。battleSeed/sourceSnapshotHashはBattleResult.finalState、failureはfinalState.failure直結とし、message合成/fallbackを禁止。

- PersonDetail.statHistoryのexact集約契約を追加。run全期間の`training.stat_growth_applied` before/after chain、48週窓、current surfaceValueを固定し、null劣化を禁止。

- MockBattle replay checkpoint取得時点をMatchId予約/World RNG消費前へ固定。replayはcurrent worldを参照せず元snapshotをexact継承。
- MockBattle eventCandidatesを正規`[battle.started,battle.finished]` 2件exact orderへ固定し、BattleResultとの全重複field cross-referenceを必須化。

- PersonDetailのSprint1PersonState direct bindingを追加。currentMental/focus/techniques/learned countを同一Person state正本へ固定し、GET補完・event再構成を禁止。

- Event eventGroupのexact semanticsを固定。trainingは`training.` prefix、technique_learningはprogressed/acquiredの2種のみでmastery除外。

- GET error precedenceを固定。session/syntax/lifecycle/resource/integrity/cursorの衝突時statusを一意化。
- 0.1.13旧ページ表示文言を0.2.0 DTOへ明示同期し、所属/総合ランク/単数師匠/候補reasonの再合成を禁止。

- 再監査でGET優先順位追補の誤りを訂正。0.1.13正本の`lifecycle -> cursor署名/binding -> resource存在`を復元し、旧mock cursorはlatestなしでもSTALE_CURSOR優先とした。
- TechniqueDefinitionViewをproduction `TECHNIQUE_DEFINITION_KEYS`とexact 31-key固定。
- ApiError.validationを正本どおり`CanonicalObject[]`へ厳密化し、single object/flattenを禁止。
- pageable GETのtotalCountをfilter後・page前全件数、nextCursorを残件あり時だけに固定。

- `fieldErrors`の場所表現と分類を固定。RFC6901 request pointer + 7種FieldErrorCode + stable orderとし、messageの非決定表示用契約は0.1.13どおり維持。

- 再監査で追補側ValidationQueryの誤定義を修正。CanonicalGetQuery 0.2.0を5種exact unionへ再固定し、raw cursorを除外、ValidationQueryへkind/status/fixed sort/limitを完全materialize。

- PersonDetailへ`qualifiedMaster`表示を追加。canonical Person必須boolean直結とし、rank/relationship/eventからの推測・補正を禁止。

- mock candidate eligibilityを正常不適格とcanonical corruptionへ分離。exact predicateをliving+active+mock age/career+injuryへ固定し、source破損は500、pair-level条件はPOST専用化。

- PersonList/PersonDetail 0.2.0完全型を再宣言。list16/detail25 exact keys、ready detail statHistory必須・trainingHistory.available=trueとし、差分条項の再合成を不要化。

- 再カウントでPersonListItemView 0.2.0のexact key countを17→16へ訂正。PersonDetailは25のまま。

- MockBattleCandidateViewをexact4 keyへ固定し、`age`はvalidation済みcanonical stored current-age fieldから直接取得。derived ageはcross-check専用と明記。

- GET系errorReference allocator lifecycleを固定。serverInstanceIdはlistener bind前OS CSPRNG 1回、失敗はstartup fatal、counter枯渇はwrapなしprocess-fatal。

- （途中段階、後に更新）MockBattleMutation/View完全型をmutation5/view34で固定。その後raw BattleResult追加によりcurrent MockBattleViewはexact35へ更新済み。

- validator/cross-reference文脈の曖昧な「最低限」を除去。正規validator全条件＋current schema全重複field照合へ明文化（表示上の最低表示要件は維持）。

- BattleLogItemViewをexact40 keyと明記し、strategyCandidateScoresを`{action,score}[]`、priorityを`-1|0|1|2|null`へ正規型同期。

- Person temporaryCondition/currentMentalのexact rangeを固定。fatigue/injury=0..100、condition/confidence=±20、mental=0..50+spirit context。clamp/default/mixed-generation禁止。

- SimulationMutationView 0.2.0完全型をexact14 keyへ統合。start/reset/step success/partialのrevision/weeks/failedWeek/count/summary相関を一意化。

- TechniqueView stateをexact9 keyへ固定。progress cap=`definition.learningProgressRequired*10`、mastery=0..10000、learnedState=acquiredAbsoluteWeek null相関、structural+catalog semantic両validator必須。

- TechniqueDefinitionの確定済みenum/range/actionTraits/canonical array契約を0.1.14へ前倒し。未確認nested literalだけdirect validated DEFERRED_BINDINGに限定。

- Event一覧itemを正規EventEnvelope exact11 fieldへ固定。success data exact3、committed Event Streamだけをsourceとし、draft/mock candidate混入・縮約・invalid除外を禁止。

- ValidationResult listをissue2/item5/data3 exact keyへ完全型化。status/count/issuesとraw ValidationResultをindex単位でcross-referenceし、issue再整列を禁止。

- pageable success dataを共通exact3 `{items,totalCount,nextCursor}`へ再固定。People/Candidate/Event/Validation/BattleLogへ適用し、endpoint独自wrapper追加を禁止。

- API success/failure envelopeを0.2.0 exact5/exact6へ完全型化。ApiError allowed8 keyのpresence matrix、revision/isUpdating/refresh/errorReference相関を統合。

- （途中段階、後に撤回・再採番）DEFERRED_BINDINGを一時24件まで拡張した。模擬UI-000でStableErrorCode/SHA providerをlocal ownershipへ戻し、current台帳はDB-001～022の22件。

- cursor dataIdentity exact matrixを0.2.0へ同期。signed wrong endpoint/query.kind/prefixは400、shape-valid stale identity/revision/queryだけ409とする正本分類へ§5Aを訂正。

- （途中段階、後に撤回・再採番）StableErrorCode / training.action_selected / relationshipを台帳追加したが、StableErrorCodeはdeferred対象外と判定。currentはtraining=DB-021、relationship runtime binding=DB-022。
- TrainingHistory anchorのtargetStat/targetTechniqueId null相関を4 action exactに固定し、forced/forcedReasonはupstream validated payload direct projectionへ限定。

- 再機械監査でDB-022/023のchecklist物理row漏れと受入条件136の旧DB-001～021表記を修正。spec/checklistともDB-001～022 exact 23 rowへ同期。

- 個別DEFERRED_BINDING記述へDB-ID併記を義務化し、GATE-013で本文→registry subjectの1対1 coverageを機械監査。FIX-072旧21件表記も23件へ修正。

- 0.1.13→0.1.14破壊的変更をMIG-001～035へ集約。GATE-014でproduction旧symbol/query/field=0、新実装/negative migration test全件を機械監査。

- Person age sourceを3 view共通化。livingは保存currentAge直結＋computeCurrentAge cross-check、deceased age=null/death fields direct、update中も同一read generationを必須化。

- 後続監査再適用: relationship DB-022、cursor HMAC-before-schema、Mock A/B ordered input、repeated new mock DET-007、MockBattleView raw BattleResult exact35を反映。
- 後続監査再適用2: techniques held-only/focus、failure lastOperation、UiSession CSPRNG atomic生成、uiRevision capacity、commitState/committedWeeks matrixを反映。
- 後続監査再適用3: TrainingHistory sourceProcessor隔離、Sprint0/CLI normalizer共有、replay saved-source、POST errorReference受理境界、GET commitState noneを反映。
- （途中段階）DEFERRED_BINDINGを24件へ同期しMockBattleView exact35へ変更した時点があった。後続監査でDB=22、MockBattleView=exact34へ再修正済み。DET-007監査節は維持。

- start/reset world-scoped/session-scoped境界を追加。resetはworld/run-init/validationを置換しmock latestをclearするがsession/csrf/cursor secret/request journal/revision counterは保持。stepはlatestを保持。
- reset後のold signed cursorはsecret rotationせずSTALE_CURSOR、old journal same request replayはold bytesだけ返してcurrent world/latest/lastOperationを巻き戻さない。

- 最終整合監査: 現行集計値を acceptance159 / BRIDGE109 / TX80 / PAGE11 / DET7 / FIX94 / DB22 / MockBattleView35 へ同期。途中値は履歴であることを明記。

- 最終横断監査で受入条件116/127の旧契約を修正。GETはcursor-before-resource/HMAC-before-schema、MockBattleViewはraw BattleResult込みexact35へ同期。

- UiReadSnapshot 0.1.0のrunInitializationSnapshot欠落を検出。0.2.0 exact7へ上書きし、更新中WorldSummaryをoperationStartReadSnapshotだけで完結可能に修正。MIG-036追加。

- 正本再照合でresetのRunInitializationSnapshotをnewへ置換する誤記を訂正。resetは保存済みsnapshotだけから再初期化し、snapshot canonical valueをexact維持する。old cursorはsimulationId変化ではなくuiRevision増加だけでもSTALE。

- reset snapshot訂正時のstart bulletへの誤置換を修正。startはnew RunInitializationSnapshot、resetだけsaved snapshot exact維持。受入159もsimulationId変更を仮定せずuiRevision/result bindingでSTALEへ修正。

- cursor HMAC署名対象を0.1.13正本へ再同期。decoded payload bytesではなく第1base64url segment ASCII bytesを署名対象とし、HMAC成功後にpayload decode/canonical JSON/schema分類する順序へ訂正。

- 0.1.13正本再照合でsession ID collision no-retry契約を撤回。既存ID衝突時再生成を復元し、`SESSION_ID_GENERATION_MAX_ATTEMPTS=3`へ具体化。cursor HMAC key/sessionBindingKeyもprocess-level別鍵として§4Bへ分離。

- 0.1.13 POST lifecycle再照合でready状態でのstart経路を追加。ready-startはnew request inputからnew run/new RunInitializationSnapshotへ置換し、resetのsaved snapshot exact再利用と分離。old cursor/journal挙動も§6L/PAGE-012へ固定。

- POST precedence再監査でuiRevision capacity preflightの挿入位置を固定。domain/pre-start validation通過後・first mutation直前とし、422/404/409をcapacity 500で上書きしない。

- 0.1.13 atomic response commit再照合で、atomic start/reset/mock/replayのcommitState=complete誤契約を撤回。success DTO/JSON bytes + next state + uiRevision + completed success journalをcommit前検証し1 atomic commit、app-level failureはnoneのみ。partial/complete 500はstepだけへ限定。

- 0.1.13 isUpdating正本を追補へ明示同期。accepted POST final errorはlock保持中にbytesを構築するためisUpdating=true、POST successはfalse特則、saved responseは現在lock状態に関係なく元body exact。

- 0.1.13 cursor正本再照合でbattle-log cursorのresult identity/uiRevision二重bindingを固定。result revisionはdataIdentityの1か所だけ、payload.uiRevisionは全cursor共通。通常step後latest保持でもold log cursorはSTALE。同時にMockBattleView実型へraw battleResult fieldを実追加しexact35へ修正、DB/MIG監査の旧件数も同期。

- 正本再監査で途中追加したMockBattleView raw BattleResultを撤回しexact34へ復帰。詳細logは専用pagingだけとし、BattleLogListDataViewは正本のresultUiRevision付きexact4へ復元。session uiRevisionとresultUiRevisionがstep後に異なり得る契約を明示。

- GET latestのsession revision/result revision分離を明示。new mock/replay直後は等値、通常step後はlatest保持により`sourceWorldUiRevision <= resultUiRevision < response.uiRevision`が正常。

- validationOccurrence lifecycleを正本へ明示同期。start/reset成功は初期化結果を1から再採番、週commitのみnext値を消費し、rollback/noncommit/failed week/mock/saved replayは欠番を作らない。

- 最終機械監査で監査説明中の旧DB23/MIG35表記をcurrent DB22/MIG36へ同期。実契約変更なし。

- CursorPayload `sessionBindingHash`の型表記を既存意味契約へ同期し、non-empty stringからexact lowercase 64-hexへ厳密化。wire意味変更なし。

- WorldSummary reset source表現を§6Jへ同期。worldは再初期化後snapshot、RunInitializationSnapshot canonical valueはreset前とexact同一。

- replay source clear後のnew requestとold journal replayを分離。start/reset後new requestId replayはlatest不存在、clear前に完了済みsame replay requestIdはresource再評価せず旧response bytesをjournal exact replay。

- 最終機械監査でspec fixture一覧FIX-063のexact35旧表記をcurrent MockBattleView exact34へ同期。

- cursor dataIdentity suffix canonical lexical contractを追加。signedでも非canonical suffixは400、canonical syntaxでcurrent target値だけ違う場合に限り409 STALE。検証順のresultUiRevision直field誤読も除去。

- statHistoryへDB-011 weekly training sourceProcessor整合を追加。eventType名だけでtraining由来と推測せず、foreign processorのstat_growth eventは500。

- UI-000 readinessとfuture implementation evidenceをphase分離。UI-000はowner/test/fixture planまで、actual implementation/PASSは各owner task、UI-010で全implementedを要求。
- task責務/依存を再固定し、UI-001 live session先食い、UI-002 mock facade先行、API owner重複を禁止。GATE-015 static auditを追加。
- PAGE-007物理定義を追加し、BattleLog exact4/resultUiRevision正本に反する旧exact3/禁止文言を削除。

- cross-cutting contract ownerの意味を修正。ownerTaskはexclusive code ownerではなく最終acceptance evidence owner。前段API taskは必要scopeをconsumerEvidence付きで実装し、future endpoint先行実装は禁止。TX-070/072/073やPAGE-007等の依存逆転解釈を防止。

- UI-001～010単独実装可能性を逆監査し、task-specific Cursor instruction 10本と`S1_5_TASK_IMPLEMENTABILITY_AUDIT_0.1.0.md`を追加。
- UI-005 historyはDB-012 committed Event Stream internal readでAPI-009未完成でも実装可能と明示。UI-006 isolated runner要否はUI-000で事前確定へ固定。
- STOP分類を`spec_fix_required / code_fix_required / dependency_blocker / environment_blocker`へ拡張。UI-000 Runbookの重複章番号を解消しcross-cutting owner auditをrelease gate前へ配置。

- checker self-testで3種の意図的drift（UI-005 DB-012 rule削除、API-011 owner改変、Runbook章番号重複）を全てrejectすることを確認。

- 実装時失敗系の手戻り防止として`S1_5_FAULT_INJECTION_MATRIX_0.1.0.md`を追加しFI-001～076をUI-001～010へ割当。Cursor acceptance report exact schemaとUI-000 finding resolution matrixも追加。
- STOP分類をUI-001～010 exact4（dependency/environment/code/spec）へ統一。UI-000だけsemantic一致のphysical差異を`binding_update`として再binding可能に分離。

- 新規FI/STOP/report検査についてstatic checker自己テスト3/3を実施。FI行欠落・task FI割当欠落・STOP schema欠落をすべて検出。

- 模擬UI-000でDEFERRED_BINDING台帳を24→22へ正規化。StableErrorCodeとUI server SHA-256 providerはSprint 1.5 local ownershipへ移動。DB-014はclosed union強制をやめfinal upstream exact type bindingへ修正、DB-022 relationshipはS01-008後runtime collection/validator接続だけへ限定。

- R3 mock-UI000 checker自己テストを追加。mock DB行欠落・StableErrorCode deferred再混入・UI-010 DB重複を3/3検出。累積self-test 6/6。

- 再模擬UI-000監査でRunbook/Checklistに残ったDB24/DB025旧表記をcurrent DB22/next DB023へ修正。negative fixtureとしてのunregistered DB-023は意図的に維持。

- 再監査で0.1.13基礎正本がR3 package単体checker対象外だった点を修正。`S1_5_BASE_AUTHORITY_GATE_0.1.0.md`と`S1_5_SPEC_FREEZE_POLICY_0.1.0.md`を追加し、GATE-015をrepo-bound auditへ強化。
- Implementation PlanとCursor共通templateの重複章番号を解消し、checkerでexact numeric heading重複を禁止する方針へ変更。

- 再監査でtask instructionとimplementability auditのCritical DB/API相互照合をstatic checkerへ追加。UI-010だけでなくUI-001～010全件をexact比較。

- 2回目模擬UI-000監査を`S1_5_UI000_MOCK_AUDIT_0.2.0.md`として追加。DB次番号残存、base authority非repo-bound、workflow章番号重複、task DB/API cross-check不足を修正。

- R4再監査: Runbook/ChecklistのDB22次IDを再確認し、基礎正本0.1.13のrepo-bound gate、spec freeze policy、workflow章番号一意性、task instruction↔implementability auditのCritical DB/API exact cross-checkを追加。
- static checker self-testをR4で累積12ケースへ拡張（document corruption 8 + repo-bound base authority 4）。

- R3 Cursor指示書ZIPがtask文書の参照先を全同梱していなかったため、`S1_5_EXECUTION_BUNDLE_MANIFEST_0.1.0.md`を追加し、R4ではUI-000～010 execution bundleを自己完結化。外部依存はrepo-boundで検証する基礎正本0.1.13だけ。

- execution bundle manifest欠落testをchecker自己テストへ追加し、Implementation Plan参照欠落を検出。R4累積self-test expected 13/13。

- execution bundleをchecker単体実行できる観点で再監査し、checkerが読むcurrent mock auditとbundle manifest自身の同梱漏れを発見。必要集合を26→28件へ修正。

- API状態遷移監査でGET /sessionとlastOperationのwire ownership混線を修正。API-001 dataはsessionState/csrfToken/activeOperation exact3、lastOperationはAPI-006 GET /simulationだけ。
- GET /session bootstrapをnon-mutating cookie inspect→query validation→必要時のみsession生成へ固定。valid existing sessionのinvalid queryでも整数revision/isUpdatingを保持し、invalid requestでnew sessionを生成しない。
- API-012同一人物指定をstrict body validationの受理前400へ固定し、journal/updateControl/eligibility/RNG/MatchId消費0を明記。

- API状態遷移再監査でTX-083に残っていたatomic start `complete internal`旧ケースを削除。start/reset/mock/replayはapp-level failure=noneのみ、postcommit transport faultはsaved 200維持へ再同期。
- new session bootstrapもDTO/schema/JSON/Set-Cookieをstore登録前にprebuildし、登録後serializer failureという中間状態を禁止。
- POST precedenceへrequired canonical source/saved record integrityをcapacity前に明示。corrupt reset/replay sourceをcapacity 500で隠さない。

- API POST状態遷移監査でrequest journal acceptance順の曖昧表現を修正。strict DTO後はexisting journal lookupのみ、running record/updateControl/opStart snapshotの受理境界はlock/revision/lifecycle/resource existence通過後。domain/integrity/capacityは受理後failureとしてjournal保存可能。

- API-001～015状態遷移をST-001～015として`S1_5_API_STATE_TRANSITION_AUDIT_0.1.0.md`へ固定。各API owner taskへ同番号STを割当し、happy pathだけでなくprecondition/source/commit/failure/retryの受入証跡を必須化。

- API state-transition再監査でauthentic cookie + corrupt UiSessionの扱いを固定。invalid cookie/401またはbootstrap new sessionへ自己修復せず、500 INTERNAL_ERROR・uiRevision=null・isUpdating=false、row保持/rotation0。

- API failure/retry再監査でFault Matrixの`FORBIDDEN_ORIGIN`/`FORBIDDEN_CSRF`曖昧表記を正本`REQUEST_FORBIDDEN`へ統一。Host pre-sessionはrevision null、Origin/CSRFはvalid session metadataを維持する。

- R5 API状態遷移監査をAPI-001～015 / ST-001～015で実施。session/lastOperation wire ownership、bootstrap precedence、new-session response/store境界、POST journal受理順、source integrity precedence、same-person mock preaccept、TX-083 atomic complete誤記、preset順序、corrupt UiSession、REQUEST_FORBIDDEN表記を修正。
- static checker self-testをR5で20/20まで拡張し、今回の状態遷移契約の逆戻りも検出可能にした。

- cross-operation監査で0.1.13正本のrequestId namespace全更新endpoint共通・running/completed journalのsession/process終了までno-evictを追補へ明示。endpoint別journal/TTL/LRU/途中clearを禁止。
- step K=0 HTTP200 partial_failureがuiRevision不変のままlastOperationRequestIdだけ更新するため、uiRevisionをUiReadSnapshot full-generation ID/cache keyとして扱えないことを明示。same revisionでもGET /simulation lastOperation更新、次operationStart snapshot再captureを必須化。
- mock/replayのrevision-only cursor invalidationとK=0 partial_failureのcursor維持を対照契約として追加。

- SCN-001～014 cross-API scenario auditを追加。start/step/mock/replay/reset、K=0 partial、global requestId namespace、concurrency precedence、cursor lifetime、process restart、validation occurrenceを連続操作で受入する。

- cross-operation再確認でK=0 HTTP200のjournal/lastOperation/updateControl完了境界を明示。running→completed、saved 200 bytes、lastOperationRequestId更新、updateControl解除を不可分化し、same revisionのmetadata中間状態を禁止。

- cross-operation再監査でprocess restart old cookie/cursor status matrixを固定。旧cookie非bootstrap=401、bootstrap後empty+旧cursor=409 SIMULATION_NOT_STARTED、new run開始後の旧process cursor=HMAC不成立400とし、同一processのsemantic STALE_CURSORと分離。
- saved response replay/requestId conflictはsecurity + strict DTO validation後のみ到達可能と明記。既存requestId文字列を含むinvalid requestがsecurity/400を迂回してold responseを取得する実装を禁止。

- cross-operation再監査でsaved response header exact過剰契約を修正。0.1.13正本どおりidempotency exact対象をoriginal HTTP status + response body bytesへ限定し、非決定的transport headerは一致対象外。stable API header契約は通常どおり要求するがjournal固定保存を要求しない。

- cross-operation監査で基礎正本のclient disconnect/AbortSignal/reload契約を追補へ明示。running受理後のmutationはbrowser切断でcancel/rollbackせずserver commit/journal境界まで継続し、同requestIdでrunning確認/完了response取得する。process crashだけはresume保証外として分離。SCN-015を追加。

- cross-session requestId namespaceも明示。全mutation endpointでは同一UiSession内共通だが、別UiSession間は独立し、process-global requestId Mapによる誤衝突/誤replayを禁止。

- R6 checker self-test 30/30を再実行。途中でjournal no-evict mutationを見逃すchecker scope不備を発見し、§6N.2 exact-section検査へ修正後に全30ケースを再実行してPASS。

- R7 pre-binding: accepted S01-008 documentation（commit `7c47847`）とS01-009 clarifierを取り込み、DB-001～022のS01-008実装待ちを0へ縮小。pre-binding status/countをcheckerでexact検証し、repo-bound前のmatched誤格上げを禁止。

- Returned S01-009 evidenceによりDB-018決定性契約を修正。R6の`simulationId/eventId除外`と`CLI/UI same helper実装共用`要求は過剰・誤り。05/S01-009 authorityではsame-seedのsimulationId/eventId/sequence等は決定的比較対象で、S01-009 comparatorはverification-private。UI-009は同じsemantic rule-setを独立実装可とし、DB-018をS01-009 completion待ちからS01-008 repo exact bindingへ変更。

- DB-018修正を派生資料へ同期。BRIDGE-104/FIX-089/ACC-154/UI-009指示書を、same-seed deterministic IDs保持・05/S01-009 authority rule-set・verification-private helper非依存へ更新。

- mock audit冒頭をR7 current statusへ同期。S01-008 final implementation待ちというR6旧表現を除去し、current pre-bindingは5/13/2/2・matched0を正とする。旧candidate_evidence等はhistorical progressionと明記。
- R7 DB-018追加mutation testは6/6。初回にTX-075 checklist単独後退をcheckerが見逃したため、TX-075 exact section検査を追加して再実行6/6。

- R8 accepted-commit physical binding runnerを追加。`git show/git grep 7c47847`でS01-009 worktreeを触らずaccepted S01-008 treeを監査し、DB-001～022のmachine evidence JSON/Markdownをrepo外tempへ出力する。runner自身はmatchedを0固定し、UI-000だけがsemantic review後にmatchedへ昇格可能。
- Returned source artifactsからDB-001/008/009/010/014/017のexact module/symbol/test候補をpre-binding auditへ追記。

- R8 repo binding runnerはNode `.mjs`を正本実装、PowerShellは38行の起動wrapperへ簡素化。Node runnerは仮Git repoで実行可能なため、package検査に加えてruntime smoke testを行える構成へ変更。

- R8 runner runtime smoke: temporary Git repoでNode runnerを実行し、22 DB row生成・20 incomplete/2 CAL pending・worktree不変・matched0を確認。positive fixtureではDB-008/009/010がmachine_evidence_completeへ上がることも確認。
- R8 checker追加mutation 5/5を確認。

- R9 long-run Historical architectureを追加。死亡済みPersonは削除せずPersonId維持・基本immutable、normal weekly hot path/clone/full validationから除外し、relationship adjacency indexで家系/血縁をbounded traversalできる将来境界をHIST-001～056へ固定。
- Historical corruptionはfield/section/nodeへlocal degradationし、読める父名等は表示継続。rule-critical ancestryは`related/not_related/cannot_determine`三値で、missing/corruptedをnot-relatedへ補正せず個別処理をfail-closed。
- 現0.2.0 PersonDetail/relationship/statHistory whole-500契約をcurrent monolithic source限定へ明確化。Historical partial-readは将来versioned wire contract。
- UiReadSnapshotのdeep immutable semanticsをarchive全件clone非要求へ、WorldSummary.personCountをlogical active+historical count + maintained counter/index許容へ修正。
- childIds/spouseIds等のreverse linkをimmutable Historical Person payloadの唯一正本にせず、canonical relationship + adjacency indexへ分離。
- R9 scenario再監査でHSC-001～014を追加。posthumous child、10,000 historical performance counter、partial father display、missing ancestor、positive/negative kinship completeness、stale index、quarantine cache、cycle corruption、old Event/Result corruptionを検証。
- scenario passでHIST-049～053を追加し、derived index rebuildability、quarantine cache advisory、coarse corruptedでもfield salvage、cycle/depth safety、stale indexでnot-related証明禁止を固定。
- R9 Historical checker mutationを10件実行。初回missing ancestry→not_related変更をcheckerが見逃したためHIST-034 exact assertionを追加し、再実行10/10 DETECTED。
- Cursor acceptance report/UI-010へHistorical non-regression PASS/FAILを追加。archive subsystem未実装はblockerではなく、current codeがHIST禁止前提を固定していないことだけをcurrent acceptanceで確認。

- R10 Historical second deep-pass: HIST-057～068 / HSC-015～024を追加。partial-readを物理的に成立させるindependent framing、PersonDirectory locationとpayload integrityの分離、normal-load bounded metadata、stale-index hot-path full-scan禁止、traversal budget exhaustion=cannot_determine、generation-based repair/read isolation、death-transition crash recovery、append cursor、retention expiry!=corruption、supporting historical prefixのweekly wholesale clone禁止を固定。
- R10でR9 scenario documentの重複heading（旧`## 10. Scenario verdict`）も修正し、HSC document headingsを1～22 exactへ正規化。

- R10 Historical third deep-pass: HIST-069～073 / HSC-025～029を追加。segment単位failure domain、control-plane corruptionのdegraded mode、PersonId allocator/high-water integrity、content identityにbindしたintegrity cache、retention tombstoneによるID/sequence continuityを固定。

- R10 Historical fourth deep-pass: HIST-074～076 / HSC-030～032を追加。live reader中のold generation保護と安全reclaim、multi-request Historical cursor/read tokenのgeneration binding、unchanged Current->Historical referenceのvalidated evidence再利用を固定し、control-plane degraded時にunrelated weeklyが古いpayload再validationで停止する抜け道を閉じた。

- R10 final Historical checker再監査: HIST-057～076を各1件ずつ逆意味へ変えるconsolidated mutationを実行。初回17/20でHIST-057/058/066のchecker意味固定不足を検出し、exact row assertion追加後20/20 DETECTED。
- HIST-070 history-dependent aggregateのactive-only偽装禁止と、normal-load control-plane failureのarchive-empty/full-scan fallback禁止もcheckerへ追加し、追加2/2 DETECTED。R10 current checkerの最終Historical mutation setは22/22。

- R11 fifth-pass Historical hardening: rule-specific dependency minimality、stable placeholder identity、resumable checkpoint↔Historical generation binding、durable save pin、old-save fork/ID safety、unsupported schema分類、history-dependent write fail-closed、repair non-retroactivity、integrity trust anchor、relationship index generation binding、same-week post-death exclusionをHIST-077～087/HSC-033～043/CA-039～049として追加。
- Cursor task templateの「comparatorをUI独自に作らない」というR7以降と衝突する旧表現を、「comparison semanticsを独自発明しない／verification-private helper共用は不要」へ修正。

- R11追加再監査: marriage等のrelationship lifecycle history preservation（HIST-088/HSC-044/CA-050）と、death transitionの全生涯history materialization禁止（HIST-089/HSC-045/CA-051）を追加。

- R11 checker prose locks: HIST-084/085/087のnormative rowだけでなく9C/9D/9E本文もexact意味検査し、説明本文だけ逆転した矛盾を検出可能にした。HSC-001の`full historical payload`を`full final Person payload`へ明確化し、生涯Event/Result全materializeと誤読される余地を除去。

- R11 memory deep-pass: weekly CPUだけでなく1000年級常駐memoryを制約するHIST-090/HSC-046/CA-052を追加。Historical heavy payload全件RAM常駐を必須化せず、lazy/on-demand + bounded/evictable cacheを許容。

- R11 checker最終増分試験: 初回15ケースで12/15、HIST-084/085/087の説明本文だけ逆転する矛盾を見逃したため9C/9D/9E exact-section検査を追加。relationship lifecycle/HIST-090まで含む最終16ケースを再実行し16/16 DETECTED。

- R12 Historical sixth deep-pass: weekly processor外しだけでは残るall-history state-hash/canonicalization経路を禁止し、logical history identityとphysical storage generationを分離。
- versioned Historical semantic/catalog provenance、checkpoint publish durability、save/pin crash ordering、fork/branch GC roots、stale UI cache非authority、audit/repair分離、relationship temporal lifecycle、partial-read paging identityをHIST-091～100 / HSC-047～056 / CA-053～062として追加。

- R12 checker rangesをHIST=100/HSC=56/CA=62へ同期し、HIST-091～100の意味・9F～9L本文・HSC-047～056をexact検査するよう強化。plan/checklist/template/runbook/UI-010/mock/implementabilityのrangeとnon-regression表現も同期。

- R12 seventh deep-pass: hostile Historical decode resource bounds、duplicate logical identity conflict、semantic dependency retention、maintenance/gameplay publish lost-update防止、checkpoint one-boundary capture、orphan segment non-authorityをHIST-101～106 / HSC-057～062 / CA-063～068として追加。

- R12 consistency rescan: Compatibility Audit 3EのCA-039～052=14件に対して本文だけ`11 gap`だった不一致を14へ修正。checkerへ本文件数drift検査を追加。

- R12 final range rescan: Compatibility Audit末尾のcurrent `HIST-001..090`を`HIST-001..106`へ同期しcheckerで固定。

- R12 final checker evidence: sixth-pass initial 11/14 -> checker polarity/range fixes -> 14/14、seventh-pass 11/11、final consolidated 24/24 DETECTED。

- R12 post-review整合監査で、UI-000 Checklist machine conditionに残存していた`row/unique/matched=24`をcurrent DB-001..022契約へ同期し22へ修正。checkerへ22件exact assertと24残存rejectを追加。
- 同監査でFreeze Policyだけが`real UI-000直前freeze`、Runbook / Implementation Planが`UI-000 PASS後・UI-001前freeze`となっていた競合を解消。既存Runbook/Plan/hand-off多数派契約に合わせ、UI-000はpre-freeze proposalでphysical binding evidenceを確定し、P12 PASS後・UI-001 release前にfinal spec/package/base/evidence hashをfreezeする。UI-001～010のみfreeze record必須。外部gameplay/API/storage意味論の変更なし。

- R12 Role 2 zero-base consistency re-auditで、UI-000 Runbook §18のUI-001 release gateからpost-UI-000 freeze record/hash一致・exact frozen bytes再監査PASSの明示が落ちていた自己矛盾を修正。Freeze Policy §2とImplementation Planの既存契約へ同期し、これら4条件をUI-001開始条件へ追加。
- 同再監査でRole 2準備資料のUI-000開始順が`package-only -> base-authority -> repo-bound`となり、Runbookの固定phase順`P00 repository/base-authority -> P01 package-only + repo-bound`と逆転していた残存差を修正。外周coordination資料もP00→P01へ統一。
- static checkerへP00/P01/P02 section order、P00 base-authority ownership、P01 package-only/repo-bound ownership、UI-001 freeze release gateのsection-scope assertを追加。incremental mutation C1/C2を実行し2/2 DETECTED。外部gameplay/API/storage意味論・DB subject・契約件数は変更なし。
- R12 Role 2 zero-base follow-upで、R8/S01-008 prebinding資料に残る`S01-009 pending` / `7c47847 before final UI-000`表現がSprint 1 COMPLETE・T01後のfinal P04と混同され得る問題を修正。R8 runner/task/audit/mockをhistorical/supplemental evidence laneと明記し、7c47847 evidence単独でfinal `matched`を満たさないことを固定。
- UI-000 Runbook P00へlatest roadmap predecessor evidence（T02 complete / T03-A complete / T03-B UI-000-required residual=0）とfixed repository baseline HEAD/branch記録を追加。P04は全predecessor完了後のfixed repository stateへDB-001..022をfinal bindingし、T01等で変化したrowを必ず再照合する。
- checklist metadata / checker / mock / implementation planへ同分離を同期。incremental consistency-D mutation 3/3 DETECTED。historical accepted-commit provenanceは保持し、外部gameplay/API/storage semanticsは変更なし。

- R12 Role 2 repeated inverse auditで、base UI-000 GATE-001～010のphase ownershipがRunbookへ完全に明示されていないorchestration gapを修正。P00=GATE-001/003/007/008/009、P02=GATE-002/010、P03=GATE-004/005/006、P01=GATE-015、P04/P11/P12=GATE-011/012/013、P08=GATE-014としてactual evidence ownerを固定し、P12へGATE-001～015 all PASS tableを必須化。
- 同監査でUI-001の専用task/Implementation Plan/Implementability Auditへpost-UI-000 freeze/hash一致 + exact frozen bytes package-only/repo-bound re-audit PASSをpredecessor release gateとして明示。Common Task Templateの既存freeze preflightと同期し、意味論変更なし。
- checkerへGATE phase ownership / P12 all-gates / UI-001 dedicated release-gate assertを追加し、incremental consistency-E mutation 4/4 DETECTED。外部gameplay/API/storage/calendar意味論・DB/API件数は変更なし。

- R12 Role 2 authority-boundary再監査で、`SPEC_MIRROR.md`の「specs/proposedはGit反映前にauthorityではない」規範とFreeze Policyの`pre-freeze proposal -> UI-000 -> freeze`表現の間に、proposal-only semantic/control文書をimplementation baselineへ昇格できる余地を検出。real UI-000 P00へproposal→Git adoption完了、P00-fixed Git HEAD==specs/current manifest HEAD、proposal-only semantic/control input=0を追加。
- Base Authority Gateをbase 0.1.13だけでなくcurrent 0.1.14 amendmentのGit-tracked exact1/dirty=false/package byte-hash一致まで検査するよう強化。Freeze Policy/Implementation Planも`pre-freeze Git-reflected semantic/control revision`を実装前提へ統一。
- checkerへproposal/Git authority boundary assertを追加し、incremental consistency-F mutation 3/3 DETECTED。Git反映自体・calendar/gameplay/API/storage意味論はこのproposal監査では変更しない。

## R12 Role 2 repeated consistency pass — authority / release inheritance

- UI-001 dedicated instructionのhandoff predecessorを、UI-000 acceptedだけでなくpost-UI-000 freeze/hash一致 + exact frozen bytes package-only/repo-bound再監査PASSまで含む表現へ統一。
- `R12_PROPOSAL_MANIFEST.md` をexecution bundleへ含め、Runbook P00が参照するproposal-adoption flowをbundle外の暗黙依存にしない。
- bundleはexecution copyでありauthorityではないこと、Git-reflected semantic/control setとのbyte/hash一致を必須化。
- freeze recordへproposal-adoption evidence SHA / Git-authority mapping SHA / amendment authority path/blob/commit/SHAを追加。
- checkerへauthority mapping / bundle manifest / UI-001 release predecessorの独立assertを追加。

- proposal→Git authority mappingのactual pathはpost-predecessorまでDEFERする一方、mapping schemaを4分類＋exact evidence fieldsへ固定。未分類/duplicate/proposal-only semantic inputを0へするP00 controlを追加。
