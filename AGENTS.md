# AGENTS.md

このリポジトリで作業するAIは、以下を必ず守ること。

## 優先順位

1. `docs/SPEC.md` をゲーム仕様の正本とする。
2. 対象の `docs/specs/*.md` を実装契約とする。
3. 対象の `tasks/Sxx-xxx.md` を今回の変更範囲とする。
4. `docs/TECHNICAL_DECISIONS.md` を技術上の固定事項とする。
5. `config/` の数値は検証用暫定値であり、ゲーム仕様そのものではない。

矛盾・不足がある場合は実装を止め、独自判断で補完しない。

## docs/wiki（説明・索引）

- 実装前に正式仕様と関連テストを確認する。
- `docs/wiki` は説明・索引であり正本ではない。
- Wikiと正本が矛盾する場合は正本を優先し、矛盾を `docs/wiki/contradictions.md` へ報告する。
- 未確定仕様をWikiから推測して実装しない。
- タスク完了後、関連Wikiページの更新要否を報告する。

## 禁止事項

- 仕様にない機能を追加しない。
- 未確定事項を独自判断で確定しない。
- 対象タスクと無関係な変更・リファクタリングをしない。
- `Math.random()` を使用しない。
- シミュレーション中核からReact、HTTP、DB、ORM、ブラウザAPIを参照しない。
- 暫定値をロジックへ直書きしない。
- テストを削除・無効化して完了扱いにしない。
- エラーを握りつぶさない。
- `docs/SPEC.md` と `docs/SPEC_CHANGELOG.md` を実装都合で変更しない。
- ロックファイルを削除したり、依存バージョンを無断変更したりしない。
- `docs/TECHNICAL_DECISIONS.md`にない実行時依存を独自に追加しない。
- ID、ハッシュ、乱数、並び順の仕様を「一般的な方法」で補完しない。必ず該当ミニ仕様へ従う。

## 実装原則

- TypeScript strictを維持する。
- ESMを使用する。
- 純粋関数と依存注入を優先する。
- 入力、出力、副作用、乱数源を明示する。
- 同じ設定と同じシードから同じ決定的結果を返す。
- ID、世界日時、イベントを構造化データとして扱う。
- 1タスクにつき必要な実装とテストだけを行う。

## 作業開始前

1. 対象タスクを読む。
2. 指定されたミニ仕様と技術決定を読む。
3. 変更予定ファイルを列挙する。
4. 対象外を確認する。
5. Gitの状態を確認し、既存の未コミット変更を破棄しない。

## 作業完了前

1. 対象タスクの受入条件を確認する。
2. 対象テストを実行する。
3. 全テストを実行する。
4. 型チェック、lint、format check、buildを実行する。
5. 再現性が対象となるタスクでは同一シード比較を実行する。
6. 変更範囲外の差分がないことを確認する。

## 完了報告

```text
対象タスク:
実装した内容:
変更ファイル:
追加・更新したテスト:
実行したコマンド:
テスト結果:
仕様との差異:
未解決事項:
次タスクへの影響:
```

差異や未解決事項がない場合も「なし」と明記する。
